# build output
dist/
# generated types & cache
.astro/
# dependencies
node_modules/
# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
# environment variables
.env
.env.production
.env.*.local
# macOS
.DS_Store
# editor
.vscode/
.idea/
# cloudflare worker (added in Phase 1)
worker/node_modules/
worker/.wrangler/
# large raw datasets (we commit pruned JSON, not raw dumps)
scripts/data-raw/
# Phenol-Explorer raw export — a local build input for scripts/build-phenol-explorer.mjs.
# We commit the derived src/data/polyphenols.json + src/data/phenol-crosswalk.json, not
# the full upstream database (whose redistribution is gated).
src/data/complete_composition_data.csv
src/data/foods_having_composition_data.csv
